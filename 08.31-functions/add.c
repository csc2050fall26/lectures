/* NOTE: This defines a function that takes two integers and returns an integer;
 *       the curly braces create a new block, and thus this is also a valid
 *       place to define new local variables. */
int add(int x, int y) {
    int z = x + y;

    return z;
}
