/* NOTE: This defines a function that takes two integers and returns an integer.
 *       Note that the beginning of a function definition creates a new local
 *       scope, and is thus a valid place to declare local variables. */
int add(int x, int y) {
    int z = x + y;

    return z;
}
