#include <stdio.h>

/* NOTE: In C, the entry point is the function "main"; its arguments are the
 *       command line arguments. If they are not needed, they can be declared
 *       as "void" to reassure the compiler that we didn't just forget about
 *       them. */
int main(void) {
    printf("Hello, world!\n");

    return 0;
}
